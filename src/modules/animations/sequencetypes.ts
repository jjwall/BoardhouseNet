/**
 * List of all possible animation sequences.
 */
 export const enum SequenceTypes {
    DEFAULT = "DEFAULT",
    IDLE = "IDLE",
    WALK = "WALK",
    RUN = "RUN",
    ATTACK = "ATTACK",
    ACTION_HOLD = "ACTION_HOLD",
}