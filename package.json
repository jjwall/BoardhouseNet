{
  "name": "boardhouse-node",
  "version": "1.0.0",
  "description": "A full-stack environment for the boardhouse game framework.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build-lobby-server && npm run build-lobby-front && npm run build-boardhouse-back && npm run build-boardhouse-front",
    "build-front": "npm run build-lobby-front && npm run build-boardhouse-front",
    "build-back": "npm run build-lobby-server && npm run build-boardhouse-back",
    "build-lobby-server": "webpack --config webpack-lobby-server.config.js",
    "build-lobby-front": "webpack --config webpack-lobby-front.config.js",
    "build-boardhouse-back": "webpack --config webpack-boardhouse-back.config.js",
    "build-boardhouse-front": "webpack --config webpack-boardhouse-front.config.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jjwall/boardhouse-node.git"
  },
  "author": "Jacob Wallace",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jjwall/boardhouse-node/issues"
  },
  "homepage": "https://github.com/jjwall/boardhouse-node#readme",
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@types/node": "^12.7.12",
    "@types/ws": "^6.0.3",
    "child_process": "^1.0.2",
    "copy-webpack-plugin": "^5.0.4",
    "ts-loader": "^6.2.0",
    "typescript": "^3.6.4",
    "webpack": "^4.41.1",
    "webpack-cli": "^3.3.9",
    "webpack-node-externals": "^1.7.2"
  },
  "dependencies": {
    "express": "^4.17.1",
    "three": "^0.109.0",
    "ws": "^7.1.2"
  }
}
